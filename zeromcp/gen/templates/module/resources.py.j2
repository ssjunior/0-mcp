"""Resources for the ``{{ module_name }}`` module — generated by `0-mcp init`.

Each ``BaseResource`` subclass exposes one model as REST + MCP. The
generator emits the minimum (``model = X``) and (when relevant) the
list of columns deemed sensitive by name pattern. Add ``filter_fields``,
``search_fields``, ``mcp_fk_expand`` etc. by hand when you need them.
"""
from zeromcp import BaseResource

from .models import (
{% for class_name, _, _ in members %}
    {{ class_name }},
{% endfor %}
)


{% for class_name, tcfg, _ in members %}
class {{ class_name }}Resource(BaseResource):
    model = {{ class_name }}
{% if config.project.read_only %}
    allowed_methods = ['get']
{% endif %}
{% if tcfg.sensitive %}
    sensitive_fields = {{ tcfg.sensitive }}
{% endif %}


{% endfor %}
